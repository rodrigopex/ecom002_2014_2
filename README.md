ecom002_2014_2
==============

Repositóri novo:
* git clone url
* ...programa/altera
* git status 
* git add --all
* git commit -m "mensagem"
* git push


Repositório já clonado:
* git pull
* git status 
* git add --all
* git commit -m "mensagem"
* git push
